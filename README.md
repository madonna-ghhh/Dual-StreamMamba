# Dual-StreamMamba
Dual-Stream Mamba EEG (v2) is a per-patient seizure-prediction model combining temporal and spectral Mamba SSM branches with gated fusion. It uses a bottleneck classification head, stochastic depth, and stochastic weight averaging (SWA) with smoothed early stopping to curb overfitting on small, largely synthetic per-patient EEG datasets.
